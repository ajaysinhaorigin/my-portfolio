import type { ReactNode } from "react";

/**
 * Explore section uses its own chrome.
 * Topic layouts replace the portfolio navbar with a docs-style shell.
 */
export default function ExploreLayout({ children }: { children: ReactNode }) {
  return children;
}
